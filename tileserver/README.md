# TileServer-GL

TileServer-GL serves processed GeoTIFF raster layers (hydrological overlays) over
Mapbox GL via the TileJSON protocol. It runs on **port 8080**.

Place `.mbtiles` archives under `data/mbtiles/` and GeoTIFF rasters under
`data/geotiffs/`. Styles live in `styles/`.
